import { SvgIcon } from '@mui/material';
import LogoIcon from '@/public/static/Logo.svg';

const Logo = () => (
  <SvgIcon component={LogoIcon} viewBox="0 0 160 40" sx={{ fontSize: 150, height: 64 }} />
);
export default Logo;
