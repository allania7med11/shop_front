import { Box, SxProps } from "@mui/material"
import { FC } from "react"




export const Filters: FC<{ sx:SxProps }> = ({sx}) => {
    return (
        <Box sx={sx}>
            Filters
        </Box>
    )
}